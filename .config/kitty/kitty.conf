
background_opacity 0.85
# Kitty config file
# Sunset-inspired color scheme

# Background color (deep purple-black to match the darkest part of the sunset)
background #1E1328

# Foreground text color (light sunset peach for visibility)
foreground #F7E3D8

# Cursor color (vibrant sunset orange)
cursor #FF6F61

# Selection background (radiant pink to reflect the sunset glow)
selection_background #8A3B9A

# Selection foreground (same as foreground for visibility when selected)
selection_foreground #F7E3D8

# Color of bold text (soft sunset orange)
bold #FF9B6B

# Color of links (light pink to match the sunset's highlights)
link_color #E99AC1

# Tab bar background (matching the background color)
tab_bar_background #1E1328

# Active tab foreground (pale orange for active tab text)
active_tab_foreground #FFCC99

# Active tab background (slightly darker purple for active tab background contrast)
active_tab_background #2A0E43

# Inactive tab foreground (muted purplish grey for inactive tab text)
inactive_tab_foreground #7A667A

# Inactive tab background (same as background for consistency)
inactive_tab_background #1E1328

# Terminal color scheme
# Black (darkest background color, matches the deep parts of the sunset)
color0 #151024

# Red (vibrant sunset orange for warning text or highlights)
color1 #FF6F61

# Green (light and fresh, for success messages or accents)
color2 #66FF8F

# Yellow (soft gold to reflect the light from the sunset)
color3 #FFD773

# Blue (a soft sky blue from the sunset reflection)
color4 #6CB9FF

# Magenta (a pinkish hue, matching the sunset glow)
color5 #E99AC1

# Cyan (light blue for more muted sky tones)
color6 #6DC0D3

# White (light peach, matching the foreground text)
color7 #F7E3D8

# Bright terminal colors
# Bright black (deep purple, for background shading)
color8 #3B243F

# Bright red (lighter sunset orange for bold accents)
color9 #FF9B6B

# Bright green (more vivid for standout success or highlights)
color10 #A4F9C6

# Bright yellow (brighter golden tones for emphasis)
color11 #FFDFAA

# Bright blue (sky blue, a bit more intense for standout elements)
color12 #94CFFF

# Bright magenta (light pink for bright highlights)
color13 #F0BFE0

# Bright cyan (lighter blue for clean accents)
color14 #A5DAE9

# Bright white (very light peach for high visibility)
color15 #FCEDE7

# Font settings (customize as needed)
font_family  FiraCode
bold_font    auto
italic_font  auto

